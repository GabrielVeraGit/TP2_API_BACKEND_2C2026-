# Requerimientos para el proyecto. 
#   Para instalar todos los requerimientos en terminal(con .venv activo): pip install -r requirements.txt
#   Para guardar nuevas instalaciones de paquetes en el archivo requirements.txt: pip freeze > requirements.txt

# Para el Framework Flask. 
#   Para instalar individualmente por terminal(con .venv activo): pip install flask
blinker==1.9.0
click==8.5.0
Flask==3.1.3
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
Werkzeug==3.1.8

# Para la carga de variables de entorno desde un archivo .env
#  Para instalar individualmente por terminal(con .venv activo): pip install python-dotenv
python-dotenv==1.2.3